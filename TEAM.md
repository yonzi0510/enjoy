# TEAM.md — 에이전트 팀 운영 지침서

이 저장소는 **역할별 에이전트 팀**으로 작업한다. 새 세션에서도 이 문서대로 팀을 꾸리면
이전과 동일한 방식·품질로 작업할 수 있다. (규칙의 원본: `CLAUDE.md`, 디자인 기준: `DESIGN.md`)

> **가장 중요한 규칙 — 회의 먼저, 목업 보고, 승인 후 착수.**
> 개발 요청이 들어오면 **바로 만들지 않는다.** 팀회의 → 기획안 목업으로 부모님께 보고 →
> 승인 → 그때 제작팀 투입. 자세한 절차는 아래 "0단계".

---

## 조직 편성 (5개 팀 · 16명)

```
본부장 (부모님)
└ 대리인 (메인 세션)
  ├ 💡 아이디어팀 (5) — 게임 개발 · 지속 업그레이드   [핵심]
  ├ 🎨 디자인팀   (5) — 어플 전체 디자인              [핵심]
  ├ 🔧 제작팀     (앱당 1명 · 변동) — 지시서대로 구현
  ├ 🛡️ 품질·운영팀 (4) — 지키는 자리
  └ 🔍 검토팀     (1) — 통합 회귀 · 커밋 · PR
```

### 💡 아이디어팀 — 무엇을 만들지 정하고, 만든 것을 계속 좋게 만든다

| 코드 | 팀원 | 담당 | 산출물 |
|---|---|---|---|
| `IDEA-L` | **기획 총괄**(팀장) | 제안 취합 → 본부장 상신(보통 5개), 승인분을 **제작 지시서**로 변환, 우선순위·보류 결정 | 라운드 제안서, 제작 지시서 |
| `IDEA-1` | **교구 발굴** | 실물 교구 사진·영상을 분석해 놀이 방식 추출. 조작을 **한 가지로 압축**(돌린다/끼운다/쌓는다/잇는다/옮긴다). 화면으로 옮길 때 버릴 것·살릴 것 결정 | 놀이 정의서 (조작 1가지·완성 조건·무벌점 구조) |
| `IDEA-2` | **학습 지도** | 전체 놀이의 학습 영역 지도 관리(색·수·글자·도형·순서·기억·소근육). 겹치는 제안 차단, **빈 영역 우선** 제안 | 학습 영역 지도, 중복 경고 |
| `IDEA-3` | **난이도 설계** | 앱당 **3단계 × 10판 = 30판** 곡선 설계. 1단계는 거의 실패 불가, 3단계는 한 번 더 생각하게. **풀린다는 것을 증명**(정답 수순을 데이터에 넣고 검사기가 실제로 시뮬레이션) | 30판 설계안, 정답 수순 |
| `IDEA-4` | **업그레이드·확장** | 이미 나간 놀이 개선 발의(아이 반응·부모 지적 반영), 내용 늘리기(**기존 것 불변, 추가만**), 아이가 안 하는 놀이 원인 확인 | 개선 제안서, 확장 데이터 |

### 🎨 디자인팀 — 전부 한 공방에서 나온 물건처럼 보이게 한다

외부 이미지·음원을 쓰지 않으므로 **전부 직접 그리고 직접 만든다.**

| 코드 | 팀원 | 담당 | 산출물 |
|---|---|---|---|
| `DES-L` | **디자인 시스템**(팀장) | `DESIGN.md`의 주인. 모서리·바닥 단면(chunk)·그림자·틴트 기준 관리, 기준 위반 반려, 기준 개정 발의 | DESIGN.md 갱신, 기준 구현 예시 |
| `DES-1` | **화면 설계** | **가로모드 우선** 배치 + 세로모드 대응. 패드 가로·폰 가로·폰 세로 3화면 전부 잘림 없이. 본보기와 조작 영역의 크기 배분 | 화면 배치안 |
| `DES-2` | **그림·캐릭터** | 놀이 그림을 인라인 SVG로 직접 제작, 마스코트 토끼·학습 펫 관리. **글자 없이 알아볼 수 있는가** 판단 | 그림 자산, 캐릭터 규격 |
| `DES-3` | **아이콘·색** | 앱별 아이콘 2종(192·512), 홈 카드 색 3종(`--accent`·`--chunk`·`--tint`) 배정, 이웃 카드와 색 안 겹치게 전체 배치 관리 | 아이콘, 앱별 색표 |
| `DES-4` | **소리·모션** | 효과음 **직접 합성**(누름·정답·완성), 안내 문구와 목소리 속도, 완성 축하 연출, 오답 시 부드러운 반응 | 소리 설계, 연출 규격 |

> 아이가 글을 못 읽으므로 **소리가 안내의 절반**이다. 앱마다 따로 만들지 말고 `DES-4` 기준을 따른다.

### 🔧 제작팀 — 앱당 1명 (라운드마다 변동)

아이디어팀 **지시서**와 디자인팀 **기준**을 받아 구현. 자기 앱의 `validate-data.js`·`e2e.mjs`도 직접 작성.
**지시서 없이 착수하지 않는다.**

### 🛡️ 품질·운영팀 — 만든 것을 지키는 자리 (상설 4명)

| 코드 | 팀원 | 담당 | 합격 기준 |
|---|---|---|---|
| `OPS` | 🚨 **현장감독** | 작업자 정지 감시(60초 주기, 5분 무응답 2회 연속 = 정지 확정) → 세우고 → **작업물 실사** → 잔여분만 좁게 재투입. **처음부터 다시 시키지 않는다** | 완주율 100%, 전량 재작업 0건 |
| `QA-V` | 👁️ **눈검수관** | 배포 전 3해상도 화면을 **직접 눈으로** 확인(중간 상태 + 완성 상태). 통과 전 커밋 차단 | 배포 후 육안 결함 0건 |
| `QA-5` | 👶 **다섯살 검사관** | 글자 없이 되는가 / 본보기가 큰가 / 누를 자리가 넉넉한가 / 틀려도 안 다치는가 / 소리 반응이 있는가 | 부모 지적 재작업 0회 |
| `DOC` | 📋 **인수인계관** | 라운드 종료 시 이 문서 갱신 — 라운드 기록·보류 목록·**함정 노트** | 같은 함정 재발 0건 |

### 🔍 검토팀 — 통합 회귀 · 커밋 · PR (상설 1명)

검문 통과분을 전체와 함께 회귀 실행, 루트 홈 등록·문서 갱신·커밋·PR 담당.
**개별 팀은 커밋하지 않는다.**

---

## 0단계 — 팀회의 · 목업 보고 · 승인 (신설 · 필수)

> 개발 요청(새 놀이, 큰 개선)이 들어오면 **여기서 시작한다.** 건너뛰지 않는다.

### 왜 필요한가

지금까지 "예시가 넘 작아", "글씨도 모르는 아이인데" 같은 지적이 **다 만든 뒤에** 나와서
재작업했다. 결정은 만들기 전에 받는 게 싸다.

### 절차

1. **팀회의** — 관련 팀원이 각자 자기 몫을 낸다.
   - `IDEA-1` 놀이 정의서(조작 한 가지·완성 조건) · `IDEA-2` 학습 영역 판정(겹침 여부)
   - `IDEA-3` 3단계 난이도 개요 · `IDEA-4` 기존 앱과의 관계
   - `DES-1` 화면 배치안 · `DES-3` 색 방향 · `DES-4` 소리 방향
2. **기획안 한 장으로 취합** — `IDEA-L`이 묶는다. 놀이별로 아래를 반드시 담는다.

   | 항목 | 내용 |
   |---|---|
   | 놀이 이름 | 이모지 + 이름 (예: `🎡 돌림 블록`) |
   | 한 줄 설명 | 아이가 무엇을 하는지 한 문장 |
   | 조작 | **한 가지**로 (돌린다 / 끼운다 / 쌓는다 / 잇는다 / 옮긴다) |
   | 화면 구성 | 본보기 카드 / 조작 영역 / 트레이 배치 스케치 |
   | 난이도 | 1·2·3단계에서 무엇이 늘어나는지 |
   | 학습 영역 | 무엇을 배우는가 + 기존 놀이와 겹치지 않는 근거 |
   | 색·소리 | 대표색 방향, 완성 축하 방식 |

3. **목업으로 보고** — 부모님께 **아티팩트(HTML)** 로 보여드린다. 글로만 설명하지 않는다.
   화면 구성은 실제로 보이게 그린다(SVG 스케치 또는 간단한 HTML 목업).
4. **승인 대기** — 승인·수정·보류를 받는다. **승인 전에는 제작팀을 투입하지 않는다.**
5. 승인분만 `IDEA-L`이 **제작 지시서**로 바꿔 제작팀에 넘긴다.

---

## 표준 라운드 절차

0. **팀회의 → 목업 보고 → 승인** (위 0단계 · 필수)
1. **브랜치 리셋**: `git fetch origin main && git checkout -B <작업브랜치> origin/main`
2. **정적 서버**: `python3 -m http.server 8777 &` (모든 팀이 공유 — 에이전트에게 "이미 떠 있음, 죽이지 말 것" 명시)
3. **제작팀 투입**: **동시 2명까지.** 프롬프트에 반드시 포함:
   - `CLAUDE.md`(절대 규칙)·`DESIGN.md`(가로모드 우선) 정독 지시
   - **승인된 제작 지시서 전문**
   - **담당 폴더 밖 수정 금지** (루트 `index.html`·README·CLAUDE.md는 검토팀 몫)
   - **공용 파일 수정 금지** (`shared/` — 자기 폴더에 복제해 쓸 것).
     단 **아이콘은 반대다** — `tools/make-app-icons.mjs` **한 곳에서만** 고친다. 앱 폴더에 복제하면 또 갈라진다
   - **git 커밋 금지** (검토팀이 일괄)
   - 자기 검증: validate + e2e 통과, 콘솔 오류 0, 스크린샷을 스크래치패드에 저장하고 경로 보고
4. **현장감독 상시 감시** (`OPS`) — 정지 시 아래 "정지 대응 표준"대로
5. **검문**: `QA-V` 육안 검수 → `QA-5` 5세 적합 심사. 통과 못 하면 4단계로 반려
6. **검토팀**: 통합 회귀(전 앱 e2e + 루트 홈 스모크) → 홈 카드 등록 → 문서 갱신 → 커밋·푸시
   (한국어 `요약 — 부연` 형식). **검사 통과분은 기다리지 말고 먼저 커밋해 확보**
7. **배포**: PR 생성 → 머지 → `pages.yml` success 확인 → 부모님께 대표 스크린샷과 함께 한국어 보고
8. **인계** (`DOC`): 이 문서에 라운드 기록·보류·함정 노트 갱신

### 정지 대응 표준 (`OPS`)

1. **멈춤 확인** — 산출물이 5분 넘게 그대로인 상태가 **2회 연속**이면 정지. 1회는 그냥 오래 생각 중일 수 있다.
2. **세우기** — 정지시킨다. 살려두면 뒤 작업까지 막힌다.
3. **실사** — 폴더에 뭐가 있고 어디까지 됐는지 **직접 본다.** 거의 다 끝나 있는 경우가 대부분이다.
4. **잔여분만 재투입** — "무엇이 끝났고 무엇이 남았는지" 콕 집어 새 담당자에게. 남은 게 적으면 직접 마무리가 빠르다.

---

## 에이전트 프롬프트에 늘 넣는 기술 정보

- Playwright 모듈: `/opt/node22/lib/node_modules/playwright` (`playwright install` 금지), 크로미움 `/opt/pw-browsers/chromium`
- e2e 명령: 루트에서 `node <앱>/tools/e2e.mjs` (8777 서버), practika만 `BASE_URL=http://127.0.0.1:8777/practika/`, pixel은 `PW_MODULE=... node pixel/tools/e2e.js`
- 기준 화면 3종: 패드 가로 1180×820(1차), 폰 가로 844×390, 폰 세로 390×844
- **잘림 자동 검사** 패턴: `getBoundingClientRect()`가 뷰포트 안 + `scrollHeight <= clientHeight`
- 합성 포인터로 드래그/펜 검증 (`pointerType: 'pen'`은 필기 검사용)

## 자주 쓰는 팀별 지시 요점

- **새 앱 구축**: 최근 앱(`dice/`·`rings/`·`geoboard/`) 골격 복제. 헤더(🏠·pet-slot·🗣️·⭐), 스크립트 순서 profile→앱→parent-settings→voice-settings→pet-avatar→pet→time-limit→sw-register. manifest+아이콘(SVG→Playwright PNG), `Profile.key('<앱>-playground-v1')`, `validate-data.js`와 `e2e.mjs`(새로고침 진행도 유지 검사 필수) 직접 작성. 루트 홈 카드는 검토팀이 등록
- **콘텐츠 확장**: 기존 id·순서·저장 형식 절대 불변(추가만), 검증기 최소 개수 계약을 새 기준으로 강화, 시각물 전수 렌더 눈검수
- **씬(찾기 놀이터) 리디자인**: `data-find`/`data-diff`의 id·label·level·좌표 계약 유지, defs/그라디언트 금지, `validate-scene.js` + `render-scene.js` 렌더 검수
- **펫 관련**: `enjoy-pet-v1` 필드 추가만+마이그레이션, `collection`은 `[{sp,name,at}]` 객체 배열, 모든 학습 활동은 `Pet.awardSnack(1)`(작은 완료)/`Pet.awardMeal(1)`(큰 완료)
- **가로모드**: `DESIGN.md` "가로모드 우선" 절 — `@media (orientation: landscape)`에서 grid-template-areas 2단 재배치, 세로모드 무변경

---

## 함정 노트 (넘어졌던 자리 — 반복 금지)

| 증상 | 원인 | 대응 |
|---|---|---|
| 요소가 납작하게 붕괴 (tubes 시험관) | `padding`을 **퍼센트**로 주면 **부모 너비** 기준으로 계산됨 | 뷰포트 기준 `clamp()`로 |
| 화면이 백지 (lines 안내선·캐릭터) | 화면이 보이기 전에 캔버스 크기를 계산 → 폭 0으로 조기 종료 | `showScreen()` **먼저**, 그다음 `resize()` |
| 내용이 위로 붙고 아래 텅 빔 (burger 접시) | `grid-auto-rows: min-content 1fr` 이 행을 최소 높이로 고정 | `grid-template-rows: 1fr auto` |
| 날짜가 안 바뀜 (하루 사용시간 초기화) | `toISOString()` 은 **세계표준시** — 한국(UTC+9)에서 날짜가 어긋남 | `getFullYear()/getMonth()/getDate()` 로 지역 날짜 |
| 점선 틈으로 색이 샘 (coloring) | 보이는 선과 채움 경계를 같은 면에서 계산 | 경계는 **별도 오프스크린**에 실선으로, 화면에는 점선만 |
| **e2e 통과인데 화면은 깨짐** | 자동 검사는 "동작"만 보고 "보임"은 못 봄 | `QA-V` 육안 검수를 배포 전 필수 관문으로 |
| 숨김 규칙이 안 먹음 (홈 낙서) | **CSS 우선순위** — `.doodles img`(0,1,1)가 `.dd-grass`(0,1,0)를 이긴다 | 선택자를 맞춰 준다 (`.doodles .dd-grass`) |
| 크레용 테두리가 뭉개짐 | `border-image` 9조각은 필터 걸린 SVG를 자르면 모서리가 깨진다 | **늘어나는 SVG + `vector-effect="non-scaling-stroke"`** (아래 절 참고) |
| "크레용인데 자로 그은 티" | CSS `border`로는 손그림 선이 안 된다 — 아무리 굵게 줘도 반듯하다 | 테두리 자체를 크레용 SVG로 그린다 |
| 칠이 '줄무늬'로 보임 | 해치 획의 간격·굵기가 일정하면 패턴으로 읽힌다 | 간격·굵기·기울기를 어긋나게 흩는다(타일 반복이라 값은 고정) |
| 일괄 e2e 검사가 멀쩡한 앱을 실패로 표시 | 결과 문구 형식이 앱마다 다름 — `결과: N 통과, 0 실패` / `통과 N · 실패 0` / `✅ 전체 통과 (N)` | 세 형식을 모두 받는 정규식으로 판정 |
| 스킨이 프로필 배지를 못 잡음 | `shared/profile.js`가 **인라인 스타일**(`cssText`)로 그린다 | 클래스(`enjoy-profile-badge`)를 붙이고 `!important`로 덮는다 |
| 새 브랜치 만들다 방금 쓴 파일이 날아감 | `git reset --hard` + `git checkout <branch> -- .` 조합 | 브랜치를 **먼저** 만들고 그다음 파일을 쓴다 |
| 세로로 긴 이미지를 화면에 깔았더니 가로에서 아래가 잘림 | 너비에만 맞추면 높이가 화면을 넘는다 | 틀을 화면 **높이**에도 맞춘다 (`calc((100svh - 여유) * 가로세로비)`) |
| 그림 위 표시(자물쇠·체크)만 혼자 커져 어긋남 | 크기를 `vw`(화면 너비)로 줬는데 틀이 화면보다 좁아졌다 | 컨테이너 기준 `cqw` + 부모에 `container-type:inline-size` |
| 이미지가 1.6MB | 이 환경엔 PIL·cwebp·pngquant 가 **없다** | **Chromium 캔버스**로 재인코딩 (`toDataURL('image/webp', .9)`) — 1.6MB → 0.3MB |
| **조작을 고쳐도 "여전히 선 그리기 같다"** (지오보드) | 조작만 두 번 고치고 **데이터를 안 봤다.** 퍼즐 30개 중 27개가 열린 선·여러 갈래라 **실물 고무줄로는 만들 수 없는 그림**이었다 — 뼈대가 선 그리기였던 것 | 놀잇감의 느낌이 안 나면 **조작이 아니라 데이터를 의심**한다. 실물로 만들 수 있는 모양인지부터 세어 본다 |
| 끌어다 놓기가 **어디에 놓아도 걸린다** | 걸리는 거리를 못 간격의 절반(8)로 잡았다 — 판 위 모든 점이 어떤 못에서든 8 안이라 **"빗나감"이 물리적으로 불가능**했다 | 걸리는 거리는 **간격 절반보다 작게**(6.4). 못과 못 사이에 안 걸리는 자리가 남아야 되튄다 |
| **진행도 백업이 29개 중 8개만 담고 있었다** | 앱을 만들 때마다 `js/progress.js` 에는 키를 넣었는데 `parent/index.html` 목록에는 아무도 안 넣었고, 그걸 볼 검사가 없었다. 기기를 바꾸면 대부분이 안 돌아오는 상태 | 새 진행도 키를 만들면 `PROGRESS_KEYS` 에도 넣는다. `parent/tools/validate-backup-keys.js` 가 앱들의 실제 키를 훑어 대조한다 |
| 규칙 검증기가 **앱과 같은 함수를 써서 함께 샌다** (로봇 BFS) | 맵이 풀리는지 보는 BFS 가 앱의 `step()` 을 그대로 쓰니, 벽 판정을 없애자 길찾기도 같이 새서 **검증기가 통과했다** | 규칙을 **직접** 찌르는 검사를 따로 둔다(이웃 칸에서 장애물로 한 걸음 들어가 막히는지) |
| 내부 함수로만 검사해 **포인터 배선이 죽어도 초록불** | e2e 가 `App._solve()` 같은 내부 함수만 불렀다. 배선을 통째로 끊어도 20개 중 19개가 통과했다 | 흐름당 **한 번은 진짜 마우스/터치로** 집어 끌어 본다 (지오보드 「진짜 손가락 드래그」 검사) |
| **"고쳤는데 아이 기기에선 그대로"** | `sw.js` 가 캐시를 먼저 주고 새것을 뒤에서 받았다(stale-while-revalidate). 게다가 캐시 이름이 고정 `v1` 이라 `activate` 의 청소가 **한 번도 안 돌았다** | **네트워크 우선**으로 뒤집고, 큰 배포마다 **캐시 이름의 날짜를 올린다**. 저장소는 맞는데 화면이 다르면 **캐시부터 의심** |
| **검사가 있는데 아무것도 안 잰다** | ① `display:none` 요소를 0-size 라며 건너뛰어 10개 중 9개가 헛돌았다 ② 검사 함수가 **호출되지 않았다**(twist) ③ 회전이 걸린 요소를 `getBoundingClientRect()` 로 재 크기 위계가 늘 통과 | 검사를 심었으면 **일부러 깨뜨려 실패하는지 본다**. 라운드마다 전 팀에 이 확인을 시킨다. 회전 요소의 크기는 `offsetWidth` |
| 커밋에 **다른 팀 파일이 딸려 들어감** | `git add -A` — 팀이 동시에 일하는 중이라 남의 미완성 파일까지 담긴다 | 고친 **파일을 하나씩 적어** `git add` 한다. 라운드 중에는 `-A`·`.` 금지 |
| 방금 쓴 작업이 사라짐 | `git checkout <파일>` 을 되돌리기 용도로 썼는데, 그 파일에 **아직 커밋 안 한 작업**이 있었다 | 되돌리기 전에 `git status`·`git diff` 로 무엇이 담겨 있는지 먼저 본다 |
| 칸이 안 접히고 폭만 쪼그라듦 (영어 5칸) | `flex:1.4` 는 `flex-basis:0` 이라 **줄바꿈 후보가 되지 않는다** | `flex-wrap` + `flex-basis: clamp(...)` 로 최소 폭을 준다 |
| 목소리가 기계음 | 기기의 한국어 목소리 중 **맨 앞의 것**을 그냥 썼다 | 점수로 고른다 (`VoiceSettings.koVoice()` — 네트워크·뉴럴 가점, compact 감점) |
| 음성 순서가 중간에 멈춤 | 음성 엔진이 `onend` 를 안 줄 때가 있다 | **워치독**(`1000 + 글자수*450ms`)으로 다음으로 넘기고, `seqId` 로 이전 순서를 무효화 |
| 정지 감시가 멀쩡한 팀을 "정지"로 오판 | 파일 **절대 mtime** 을 쟀다 — 아직 아무것도 안 쓴 팀이 "8438분째 정지"로 나왔다 | 감시 **시작 시각을 기준선**으로 잡고 10분 유예를 둔다 |
| `import` 가 문법 오류 | `import { chromium } from process.env.PW_MODULE \|\| '...'` — 명세는 **정적 문자열**이어야 한다 | 동적으로 고르려면 `await import(경로)` |

---

## 디자인 스킨 다루는 법 (2026-08 개편 결과)

**29개 놀이가 전부 같은 표면 공식**을 쓴다:
`box-shadow: inset ..., 0 Npx 0 var(--chunk), 0 Npx Npx rgba(var(--accent), ...)`
덕분에 앱마다 `style.css`를 고치지 않고 **`shared/crayon.css` 한 장**으로 재질을 바꿀 수 있다.
각 앱 `index.html`의 스타일시트 **맨 마지막**에 불러 덮어쓴다. 디자인을 또 바꿔도 고칠 파일은 이 하나다.

**원칙 — 틀만 바꾸고 놀잇감은 건드리지 않는다.** 바탕·카드·버튼 같은 화면 틀만 바꾸고
놀이 안의 색(구슬·물감·도형·조각)은 그대로 둔다. 바탕이 조용해져 놀잇감이 더 또렷해진다.

**크레용 선 만드는 법** (CSS로는 불가능하니 이 방법을 쓴다):
```html
<!-- 공용 defs 한 번만 -->
<symbol id="crayon-frame" viewBox="0 0 100 100" preserveAspectRatio="none">
  <rect ... stroke="currentColor" vector-effect="non-scaling-stroke"/>
</symbol>
<!-- 상자마다 -->
<svg class="fr" aria-hidden="true"><use href="#crayon-frame"/></svg>
```
`preserveAspectRatio="none"`로 상자 크기에 맞춰 늘리고, `non-scaling-stroke`로 획 굵기를 지킨다.
색은 `currentColor`라 CSS `color`만 바꾸면 된다. 크레용 질감은 `feTurbulence`(선 흔들림) +
성긴 노이즈 곱하기(왁스 결) 두 겹.

**성능** — SVG 필터는 아이콘·테두리 같은 **정적 요소에만** 쓴다. 화면 요소에 많이 걸면
낮은 사양 기기에서 느려진다.

**못 하는 것** — 한글 손글씨 폰트는 용량이 수 MB라 규칙상 못 쓴다(외부 폰트 금지).
제목을 손글씨로 만들려면 글자에 크레용 **필터**를 씌우는 방법뿐이다.

---

## 라운드 기록 (선례)

1. UI 리뉴얼 "포근한 장난감 방" + 찾기 씬 6개 그림책 리디자인 (PR #31)
2. 도형·시장·색깔 실험실 새 앱 + 산수 숫자학습 + 픽셀 도안 + 펫 방 대개편 + 전 앱 먹이 연동 (PR #32)
3. 개발팀 확장: 시장 50종·동물의 집 66종·점 잇기 30·혼색 30색 (PR #33)
4. 펫 방 가로 2단 (PR #34) → 전 앱 가로모드 우선 (PR #35)
5. 실물 교구 기반 신작 대량 제작 — 꼬치·패턴·방향색·구슬보드·컵쌓기·시험관·점잇기·주사위·고무줄·슬라이드·도넛 (PR #52~#57)
6. 돌림 블록 · 무지개 탱그램 (PR #58) → 지오보드 · 선 따라 그리기 (PR #59) — **놀이 29개 도달**
7. 조직 개편: 5개 팀 16명 편성 + **회의·목업·승인 0단계 신설** (PR #60)
8. **홈 화면 정리** — 만든 날짜순 → 놀이 성격순 **6묶음**(PR #61) → 묶음 아이콘 SVG화
   144KB→17KB (PR #63) → **상자 + 눌러서 펼치기**(한 번에 하나, 첫 묶음만 열림) (PR #62)
9. **디자인 시안 C "크레용 스케치북"** 채택 — 홈 적용(PR #64) → 공용 스킨 한 장으로
   29개 놀이 재질 통일(PR #65). 전 앱 e2e 회귀 통과
10. **학습 펫 다마고치 개편** — 이름 짓기를 걷어내고 돌보기(응가·배고픔·감기·기분·도장·봐주기)를
    넣었다. 부모님 지시 "정말 펫을 키우는 것처럼 · 하루에 한번씩 안오면 아프다던가 · 그렇게해서
    학습을 매일 유도해줘". 밥은 **학습으로만** 생기게 해 고리를 닫았고, 죽음·혼내기·죄책감 문구는
    원천 금지로 못 박았다. 세기는 부모님 페이지에서 3단계
11. **TTS 자연스럽게** — 부모님 지시 "ai 말하는거 조금 더 자연스럽게 부드럽게 못해?".
    기기 목소리를 점수로 **골라 쓰고**(네트워크·뉴럴 가점, compact 감점), 빠르기 0.92·높낮이 1.0 기본,
    이모지를 빼고 감탄사 뒤에 숨을 넣는 `say()`/`parts()` 를 공용으로 내렸다. 33곳 발화 지점 적용
12. **놀이 29개 화면 통일** (라운드 최대) — 부모님 지적 "놀이마다 페이지가 달라".
    실사 → `DESIGN.md` 수치 → `shared/screen.css` 한 장 → 앱별 좁게 → **e2e 로 못 박기**.
    곁들여: 집 단추 자리 통일, 목소리 단추를 부모님 페이지로, **부모님 페이지 신설 개편**(e2e 45),
    앱 아이콘 29장 재작업, **`sw.js` 네트워크 우선 전환**
13. **언어 토끼 · 영어 알파벳** (PR #80) — 토끼를 영어 놀이터 밖 홈으로 빼 한·영·일 셋을 맡겼고
    (낱말 961개), 영어 놀이터에 알파벳 배우기·따라쓰기를 붙였다(52자). 글씨/한글 경계도 갈랐다
15. **신작 3개 — 아이디어 회의 · 직원 다수결** (부모님 지시: "은아가 좋아할만한 게임
    아이디어 회의 하고 직원다수결로 뽑힌 3개 개발까지 쭉"). 기획자 5명이 관점을 나눠
    20개를 냈고(실물교구·소리리듬·이야기역할·발달공백·순수재미), 중복을 합쳐 후보 17개를
    직원 7명이 1·2·3순위 + 거부권으로 투표했다. 뽑힌 셋: **🦴 발굴(12점)·🤖 로봇(8점)·
    💗 마음(7점)**. 셋을 서로 다른 폴더에 동시 제작하고, 공용 파일(홈·아이콘·문서·부모님
    페이지)은 통합 담당이 마지막에 한 번에 붙였다 — 동시에 건드리면 서로 덮어쓴다.
    곁가지로 **부모님 페이지 백업에서 21개 앱이 빠져 있던 것**을 찾아 고쳤다(PR #84).

14. **지오보드를 진짜 고무줄로** — 부모님 지적 "지금은 선그리기랑 다를바가 없어서",
    "유연하게 다양한 모양으로 늘어나면서 모양이 잡혀가는, 그게 지오보드의 핵심이잖아".
    **조작을 두 번 고쳤는데 둘 다 헛짚었다** — 손가락 움직임만, 고무줄 생김새만 바꿨을 뿐
    "못을 순서대로 찍는다"는 뼈대는 그대로였다. 세 번째에야 데이터를 세어 보고 진짜 원인을 찾았다:
    **퍼즐 30개 중 27개가 실물 고무줄로 만들 수 없는 그림**(열린 선·한 못에서 여러 갈래).
    퍼즐을 닫힌 고리로 다시 그리고(퍼즐 id·별 계산은 그대로), 조작을 「줄을 집어 당기기」로 바꿨다

---

## 🔴 인계 사항 (다음 세션이 먼저 읽을 것)

### 지금 배포되어 있는 것 (`main`)
- **홈은 코드로 짜여 있다.** 그림 파일 없이 SVG(제목 글씨·크레용 테두리·아이콘 35종·낙서)와 CSS 로만 만든다.
  **상세와 함정은 `CLAUDE.md`의 "홈 화면" 절**을 먼저 볼 것.
  - 놀이를 새로 넣으려면 `index.html`의 `GROUPS` 에 한 줄, `<defs>` 에 아이콘 하나. 여섯 묶음 어디든 같다.
  - 막대를 누르면 그 자리에서 펼쳐진다(아코디언). **처음엔 여섯 묶음이 모두 접혀 있고, 한 번에 하나만 펼쳐진다.**
  - 프로필 칩 줄과 묶음 사이에 **언어 토끼**가 앉아 있다(`shared/language-bunny.js`). fixed 로 두지 말 것.
  - 가로 화면에서 묶음이 두 줄(폰처럼 세로가 짧으면 세 줄)로 갈라져 화면을 다 쓴다.
  - 고치면 `node tools/e2e-home.mjs` — 폰·패드 × 가로·세로 네 경우를 잰다.
- **제목은 은아 손글씨다.** 부모님이 보내주신 실제 글씨(심온아·김영지·심우영)를 보고
  사인펜 획·각진 모서리·찌그러진 ㅇ·글자마다 다른 크기와 기울기·낱말별 색까지 맞췄다.
  생성기 `tools/make-title.mjs`, 결과 `assets/title.svg` (2.6KB).
- 29개 놀이 화면: `shared/crayon.css` 로 **틀**을, `shared/screen.css` 로 **첫 화면 규격**(카드 크기·간격·제목·
  흩뿌리기)을 준다. `screen.css` 는 **스타일시트 맨 뒤**에 와야 이긴다. 수치의 원본은 `DESIGN.md`.
- **`sw.js` 는 네트워크 우선이다.** 화면이 크게 바뀌는 배포에서는 `CACHE` 의 날짜를 올린다
  (지금 `enjoy-cache-2026-08-09b`). 지금 기기 버전과 「새로 받기」는 부모님 페이지에 있다.
- **부모님 페이지가 설정의 유일한 입구다** — 시간 제한·앱 노출·마이크·펫 돌보기 세기·목소리·버전.
  아이 머리줄의 목소리 단추(`#btn-voice`)는 `shared/crayon.css` 한 줄로 감춰 두었다.
- 프랙티카는 배우기 안에 있으나 기본은 숨김 — 부모님 페이지에서 켜야 보인다.
  (이미지 홈 시절에는 아예 빠져 있었다. 코드로 옮기며 되살렸다.)

### 이미지 홈을 걷어낸 이유 (되돌리려는 유혹을 막으려고 적어 둔다)
손그림 시안 한 장을 깔고 투명 버튼을 얹는 방식이었는데, 두 가지가 걸려 부모님이 코드 방식으로 정하셨다.
- 패드를 눕히면 그림이 화면 너비의 **33%**만 쓰고 양옆 400px씩이 빈 종이였다. 단추도 47px 로 줄었다.
- 배우기 여섯 칸이 그림 안에 있어, 일곱 번째 놀이를 넣으려면 그림을 다시 그려야 했다.

### 남겨 둔 브랜치 3개 (지우지 말 것 — 부모님 지시)
| 브랜치 | 담긴 것 | 쓸모 |
|---|---|---|
| `claude/crayon-exact` | 손글씨 제목 첫 판(획 34개) | 지금 `tools/make-title.mjs` 의 뿌리. 자모 획 라이브러리가 여기서 나왔다 |
| `claude/crayon-rich` | 크레용 테두리·해치칠·종이결·낙서 8종·놀이 아이콘 6개 | 지금 홈의 기법 원본. 안 쓴 낙서(나무·꽃·돌고래·풀밭)와 `tools/make-app-icons.mjs` 가 아직 여기 있다 |
| `claude/ref-review` | 부모님이 보내주신 CSS 버전 | 비교용 |

### 아직 안 정해진 것
| 항목 | 상태 |
|---|---|
| 놀이 화면 **안쪽 패널** | **완료** — 29개 팀이 동시에 자기 앱을 낙서장 결로 바꿨다. 상세는 `CLAUDE.md` 의 "놀이 화면" 절. (옛 기록: **아직 못 함.** 안쪽 판은 정적 HTML이 아니라 앱마다 JS가 그린다(클래스 이름도 제각각). `shared/crayon.css` 처럼 한 장으로 덮을 지점이 없어서, 묶음 단위로 나눠 앱별로 손봐야 한다. 아이가 노는 화면이 바뀌는 일이라 묶음마다 목업 승인을 받고 진행할 것) |
| 놀이 **안쪽** 화면 결 | **다음 라운드 후보.** 첫 화면은 `shared/screen.css` 로 통일했지만, 그 **안으로 들어간 화면**은 아직 두툼한 옛 결이 남은 데가 있다(한글 놀이터 「글자 배우기」에서 눈에 띈다). 첫 화면 때처럼 **실사 → 수치 → 공용 CSS → e2e** 순으로 갈 것 |
| 영어 놀이터의 토끼(`#btn-mic`) | **남겨 두기로 결정.** 홈으로 옮기며 앱 쪽을 지우려 했는데, 지우면 「배운 단어」가 더는 안 쌓인다 — 홈 언어 토끼는 새 저장 키를 만들지 않으려고 **아무것도 저장하지 않게** 만들었기 때문. 역할을 갈랐다: 홈 토끼 = "언제든 물어보기", 앱 토끼 = "물어보며 단어 모으기". 부모님께 보고했고 답을 기다리는 중 |
| 아이콘 `구슬`↔`지오보드` | 48px 에서 아직 가장 닮은 한 쌍. `픽셀` 은 하트로 읽힌다 |
| 알파벳 `x-ray` | 사전(`english/js/dict`)에 없어 validate 경고 1건. 낱말을 넣든 X 의 예시 낱말을 바꾸든 하나 고를 것 |
| 홈 "이어서 하기" 줄 | **걷어냄** — 아이가 그 줄만 누르고 새 놀이를 안 찾았다. 대신 묶음 아코디언을 한 번에 하나만 열리게 했다(처음엔 모두 접힘) |
| **놀이 29개 화면 통일** | **완료** — 실사(29개 동일 조건 촬영·측정) → `DESIGN.md` 에 수치 못 박기 → `shared/screen.css` 한 장으로 내리기 → 앱별 좁게 → **규격을 e2e 로 못 박기** 순으로 했다. 패드 세로채움 54~99% → 72~93%. 지난 라운드가 갈라진 진짜 이유는 규격을 지킬 검사가 없었던 것이라, 이번엔 각 팀에 「일부러 규격을 어긴 CSS 를 넣었을 때 실제로 실패하는지」 확인시켰다. (옛 기록: **다음 라운드 · 최우선.** 부모님 지적 — "모든 페이지가 일관되야지. 놀이마다 페이지가 달라." 낙서장 라운드를 29개 팀에 **동시에** 맡기면서 기준을 `shared/crayon.css` 한 장으로만 주고 나머지(제목 크기·카드 비율·단추 생김새·여백·단계 표시·완성 축하)를 팀 재량으로 뒀다. 그래서 팀마다 해석이 갈렸다. **다음 라운드는 팀부터 붙이지 말 것** — 먼저 29개 첫 화면·놀이 화면을 한 판에 늘어놓고 **어디가 어떻게 다른지 실사**한 뒤, `DESIGN.md` 에 못 박을 수치(제목 clamp, 카드 min/max, 단추 크기, 여백 단계, 단계 표시 형식)를 정하고, 그 규격을 **공용 CSS 한 장**으로 내린다. 앱별 작업은 그 규격을 못 맞추는 곳만 좁게. 목업 승인 먼저 |
| ~~`connect`·`slide` e2e 1건씩 실패~~ | **해결됨** (통일 라운드에서 함께). 옛 기록: **`main` 에 이미 있던 결함** (펫 개편과 무관 — 개편 전후 3회씩 돌려 똑같이 실패하는 것을 확인). 둘 다 「낙서장 흩뿌리기」 검사로, 칸이 모두 같은 각도로 기울어 `shared/doodle-menu.css` 의 nth-child 흩뿌림이 먹지 않는다(`matrix(1,0,0,1,0,14)` — 회전 없이 세로 이동만). 위 통일 라운드에서 함께 잡을 것 |
| 옛 화살표 마크업 | **완료** — 13개 앱의 markup·CSS·JS 를 뿌리째 걷어내고 e2e 를 공용 화살표(`::before`) 기준으로 돌렸다. 되살아나지 못하게 **「옛 화살표 0개」 검사**도 심었다 |
| 부모님 페이지 낙서장 | **완료** — `crayon.css` 를 붙이고 인라인 660줄을 `parent/css/style.css` 로 뺐다. 부모용이라 글자 크기·문구는 그대로, 켜짐/꺼짐은 색+글자 둘로 표시(색약·흑백 대비). **`parent/tools/e2e.mjs` 를 새로 만들었다** — 그동안 검사가 하나도 없었다. 40/40 |
| 앱 아이콘 29장 | **완료** — 부모님이 「소품만, 토끼는 빼고」를 고르셨다. 29개가 전부 같은 분홍 토끼였고 소품은 48px 에서 3~7px 라 안 보였다. 마스코트는 **루트 홈 아이콘에만** 남기고, 같은 묶음 안 바탕 밝기를 8~52% 로 벌렸다(예전 식은 18%p 차 + `%4` 되감김으로 5개 넘는 묶음에서 색이 겹쳤다). 생성기는 **`tools/make-app-icons.mjs` 한 곳**으로 합쳤다 — 예전엔 세 갈래로 갈려 나중에 돌린 쪽이 이겼다. **고치면 48px 로 29개를 한 판에 놓고 눈으로 볼 것.** (옛 기록: 시안 3종을 만들어 승인 대기 중. 48px(런처 실크기)로 재 보니 29개가 전부 같은 분홍 토끼였다 — 소품이 3~7px 라 안 보인다. 「바탕색도 비슷하다」의 실제 원인은 밝기 차가 같은 묶음 안에서 12%p 뿐인 것(`recolor-app-icons.mjs` 의 `deep` 식). 시안은 스크래치패드 `icon-proposal/`. (옛 기록: **바탕색으로 갈라 놓음** (`tools/recolor-app-icons.mjs`). 토끼와 소품은 그대로 두고 홈 묶음 색을 깔았다. 48px 에서 묶음은 구별되지만 같은 묶음 안은 여전히 소품으로만 갈린다 — 더 확실히 하려면 토끼를 줄이고 소품을 키워야 하는데, 아이가 알아보는 그림이라 목업 승인이 먼저다 |

### 연결 안 된 도구
- **DesignSync(클로드 디자인)** — 인증 필요. 웹 세션은 터미널 로그인이 안 되니
  클로드 디자인에서 **"Send to Claude Code Web"** 으로 프로젝트를 보내야 열린다.
- **Canva** — 연결됨(브랜드 킷 없음). 결과물이 래스터라 앱 안에는 못 넣는다. 부모용 인쇄물에는 쓸 수 있다.
- **Higgsfield** — 연결되나 같은 이유로 앱에 못 쓴다.
- **Figma** — 이번 세션에 붙었다(미사용).

---

## 보류·후보 목록 (다음 라운드 후보)

- 칠교놀이 10 → 50개 확장 (10~15개씩 분할 진행 권장)
- 아이디어팀 보류분: ✨ 반짝반짝 따라하기(사이먼), 🐛 자라나라(성장 순서), 🕐 시계 놀이터,
  🎵 리듬 놀이터, 💛 마음 놀이터, 📖 이야기 공장, 🧹 척척 정리왕, 🌀 미로 놀이터,
  ↔️ 반대말 짝꿍, 🤸 몸으로 놀아요
